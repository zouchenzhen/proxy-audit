# 隐私说明

最后更新：2026-08-10

ProxyScope 是默认只监听 `127.0.0.1` 的本地工具，没有官方云端后端、用户账户、广告、
遥测或分析 SDK。维护者不会因为你运行本地副本而自动收到节点、订阅、Key 或结果。
但是，检测本身会按你的选择访问订阅服务器、IP 情报服务和测试端点。

## 本机处理和保存

| 数据 | 本机用途 | 默认保存方式 |
|---|---|---|
| 节点分享链接、UUID、密码等凭据 | 生成一次性内核配置并建立测试出站 | 任务期间保存在内存和临时配置；临时配置结束后删除；不写入结果文件 |
| 订阅 URL | 本机后端拉取订阅内容 | 不写入结果文件；服务重启后需要重新导入 |
| 脱敏节点元数据 | 预览、筛选和任务显示 | 当前后端内存；页面仅接收脱敏字段 |
| 出口 IP、ASN、位置、延迟和风险结果 | 生成历史、CSV、JSON、Markdown | `results/`，直到用户自行删除 |
| IP 情报 API Key | 调用用户选择的服务商 | Windows 当前用户 DPAPI 加密的 `config.secure.json`；API 不向浏览器回传完整 Key |
| 主题、字号和当前任务标识 | 恢复界面状态 | 浏览器 `localStorage` / `sessionStorage` |
| 内核日志 | 诊断当前任务 | `temp/logs/`；错误进入 UI 前会做简化和脱敏 |

`config.local.json` 是旧版明文兼容入口。如果已经迁移到加密设置，应手动删除旧文件。
本项目无法保护已经被其他恶意软件、管理员权限用户、调试器、系统备份或屏幕录制读取
的数据。

## 会访问的外部主体

仅启用本地模式不等于完全离线。按功能可能发生：

- 订阅服务器：收到正常的订阅拉取请求，通常能看到发起请求的公网 IP 和请求时间；
- ipify：请求通过待测节点发出，用于确认该节点的出口 IP；
- IP-API、ipapi.is、IPinfo、IP2Location、IPQS、Scamalytics、AbuseIPDB：收到被查询的
  出口 IP；需要 Key 的服务还会收到对应 Key。当前情报查询由本机直接发起，服务商通常
  也能看到本机请求来源 IP；
- 可选 ChatGPT、Claude、GitHub、YouTube 端点：探测请求通过待测节点发出，端点及其
  CDN 可能看到出口 IP、时间、TLS/HTTP 元数据；
- GitHub：首次缺少 sing-box 时，启动脚本从官方 Release 下载固定版本，GitHub 会收到
  常规下载请求元数据。

第三方如何处理数据由其隐私政策和服务条款决定。请只启用必要的情报源和端点，不要
把第三方个人信息、机密资产或未获授权的 IP 提交给这些服务。

## 删除和最小化

- 在设置中可逐枚删除或清空 Key；也可在停止程序后删除 `config.secure.json`；
- 删除 `results/` 可移除历史结果，删除 `temp/` 可移除临时日志和下载缓存；
- 浏览器站点数据中可清除 `127.0.0.1:8765` 的本地/会话存储；
- 减少“最近任务显示数”只改变索引数量，不删除已有报告；
- 发布 Issue、PR 或截图前应再次检查节点名、域名、出口 IP、路径和 Key 前缀。

## 安全部署边界

本地面板没有登录系统，安全前提是仅绑定回环地址。不要使用端口转发、反向代理、
Tunnel 或防火墙映射把它暴露到局域网或公网。多人共用机器时，应使用独立 Windows
账户并限制项目、结果和备份目录的访问权限。

发现隐私或安全问题请按 `SECURITY.md` 私下报告，不要在公开 Issue 中附带真实数据。
