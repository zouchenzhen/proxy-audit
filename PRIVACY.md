# 隐私说明

最后更新：2026-08-10

Proxy Audit 同时提供 Hugging Face 临时云端版与本地版，两者的数据边界不同。请选择与
数据敏感度匹配的方式。项目不含广告、第三方分析 SDK 或维护者自建用户账号系统，但
Hugging Face、订阅服务器、IP 情报商和测试端点会处理完成服务所必需的数据。

## Hugging Face 临时云端版

在线版开箱即用，无需下载本地引擎。用户同意后，浏览器会把待测节点、订阅 URL 或内容、
所填 API Key、设置、任务和结果上传到 `zouchenzhen/zcz` Docker Space 的同域后端。

| 数据 | 云端用途 | Proxy Audit 后端的处理方式 |
|---|---|---|
| 节点链接、UUID、密码 | 生成一次性内核配置并测试出站 | 仅保存在进程内存和任务期间的临时配置；配置结束后删除 |
| 订阅 URL/内容、上传文件 | 解析待测节点 | 在请求专属临时目录处理；请求结束后删除 |
| API Key | 调用用户选择的情报商 | 仅保存在匿名会话内存；完整 Key 不回传页面、不进入导出或日志 |
| 脱敏节点、任务和结果 | 预览、筛选、导出 | 仅保存在进程内存，不写入项目历史文件或数据库 |
| 主题、字号、匿名会话令牌 | 恢复当前标签页 | 浏览器 `localStorage` / `sessionStorage` |

- 每个浏览器标签页使用一个高熵匿名会话令牌；Proxy Audit 不要求注册账号。
- 会话最长一小时。到期或点击“立即取消并删除会话”时，令牌立即失效、任务收到取消信号
  并触发内存清理；已经开始的单次网络请求可能等待最长 12 秒超时后才完全释放。
- Space 重启、休眠或重新部署会使所有内存会话立即丢失。请检测完成后马上导出结果。
- 云端限制为每次导入/单任务最多 20 个节点、并发最多 2、全站最多 2 个活动任务、同一
  网络每日最多处理 100 个节点。高敏感、大批量或需要持久历史的场景请使用本地版。
- 后端拒绝回环、私网、链路本地、保留地址和云元数据网络，并在解析后固定公网目标 IP，
  以降低 SSRF 和 DNS 重绑定风险。

Hugging Face 仍可能按其自身政策处理访问 IP、时间、User-Agent、平台日志、滥用防护和
基础设施元数据。维护者无法代表 Hugging Face 承诺其日志保留周期。不要把云端版用于
你不能接受交给第三方基础设施处理的数据。

## 本地版

本地面板只监听 `127.0.0.1`，维护者不会因为你运行本地副本而自动收到节点、订阅、Key
或结果。

| 数据 | 本地用途 | 默认保存方式 |
|---|---|---|
| 节点链接、UUID、密码 | 生成一次性内核配置并测试出站 | 内存和临时配置；任务结束后删除；不写入结果文件 |
| 订阅 URL | 本机后端拉取订阅 | 不写入结果；服务重启后重新导入 |
| 脱敏节点元数据 | 预览和筛选 | 当前后端内存；页面只接收脱敏字段 |
| 出口 IP、ASN、位置、延迟和风险结果 | 历史和导出 | `results/`，直到用户自行删除 |
| API Key | 调用情报商 | Windows 当前用户 DPAPI 加密的 `config.secure.json` |
| 主题、字号和当前任务标识 | 恢复界面 | 浏览器 `localStorage` / `sessionStorage` |
| 内核日志 | 本机诊断 | `temp/logs/` |

`config.local.json` 是旧版明文兼容入口。如果已迁移到加密设置，建议手动删除旧文件。
本项目无法防止管理员、恶意软件、调试器、系统备份或屏幕录制读取本机数据。

## 外部数据流向

无论使用哪一版，检测都不是完全离线：

- 订阅服务器会收到正常拉取请求；云端版看到 HF 出站来源，本地版通常看到本机来源；
- ipify 和可选 ChatGPT、Claude、GitHub、YouTube 探测通过待测节点发出，端点会看到节点
  出口 IP 和常规 TLS/HTTP 元数据；
- IP-API、ipapi.is、IPinfo、IP2Location、IPQS、Scamalytics、AbuseIPDB 会收到被查询的
  出口 IP；需要 Key 的服务还会收到相应 Key；
- GitHub 可能收到下载或拉取固定版本检测内核/源码所需的常规请求元数据。

第三方如何处理数据由其隐私政策和服务条款决定。只启用必要的情报源与端点，不要提交
第三方个人信息、机密资产或未获授权的数据。

## 删除与最小化

- 云端版：完成后先导出，再点击“立即取消并删除会话”；也可直接关闭标签页并等待会话到期；
- 本地版：在设置中移除 Key，删除 `config.secure.json`、`results/` 或 `temp/` 中不再需要的数据；
- 可在浏览器站点设置中清除对应站点的本地/会话存储；
- 发布 Issue、PR、截图或日志前，重新检查节点名、域名、出口 IP、路径和 Key 前缀。

发现隐私或安全问题请按 `SECURITY.md` 私下报告，不要在公开 Issue 中附带真实数据。
